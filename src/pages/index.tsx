const HappyPos = () => {
  return (
    <h1
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Welcome to SarrMal
    </h1>
  );
};

export default HappyPos;
